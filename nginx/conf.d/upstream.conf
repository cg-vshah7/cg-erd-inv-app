upstream backend {
    server backend:8000;
    keepalive 32;
}

upstream keycloak {
    server keycloak:8080;
    keepalive 32;
}

upstream frontend {
    server frontend:80;
    keepalive 32;
}

upstream grafana {
    server grafana:3000;
    keepalive 16;
}
